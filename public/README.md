# Public Assets

This directory contains static assets served by Next.js.

## OG Image

The `og-default.png` file is a placeholder. Replace it with your actual OpenGraph image (1200x630px recommended).

## Favicon

The `favicon.ico` file is a placeholder. Replace it with your actual favicon.

